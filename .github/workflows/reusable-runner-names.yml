# AUTO-GENERATED by .github/scripts/sync-runner-names.mjs
# Source of truth: .github/runners.yaml
# Do not edit this file by hand.

name: Runner names

on:
  workflow_call:
    outputs:
      ubuntu_2004:
        description: "Ubuntu 20.04 hosted x64"
        value: ${{ jobs.export.outputs.ubuntu_2004 }}
      ubuntu_2204:
        description: "Ubuntu 22.04 hosted x64"
        value: ${{ jobs.export.outputs.ubuntu_2204 }}
      windows_2019:
        description: "Windows Server 2019 hosted x64"
        value: ${{ jobs.export.outputs.windows_2019 }}

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      ubuntu_2004: ${{ steps.export.outputs.ubuntu_2004 }}
      ubuntu_2204: ${{ steps.export.outputs.ubuntu_2204 }}
      windows_2019: ${{ steps.export.outputs.windows_2019 }}
    steps:
      - name: Export runner labels
        id: export
        run: |
          echo 'ubuntu_2004=ubuntu-20.04' >> "$GITHUB_OUTPUT"
          echo 'ubuntu_2204=ubuntu-22.04' >> "$GITHUB_OUTPUT"
          echo 'windows_2019=windows-2019' >> "$GITHUB_OUTPUT"
